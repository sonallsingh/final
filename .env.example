# Spring Boot backend URL (for chatbot patient context retrieval)
BACKEND_URL=http://localhost:8080

# Internal service token (optional — leave empty for local dev)
INTERNAL_TOKEN=

# Flask port
PORT=5000
